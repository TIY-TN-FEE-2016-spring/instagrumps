import { somethingWhere } from 'instagrump/helpers/something-where';
import { module, test } from 'qunit';

module('Unit | Helper | something where');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = somethingWhere([42]);
  assert.ok(result);
});
